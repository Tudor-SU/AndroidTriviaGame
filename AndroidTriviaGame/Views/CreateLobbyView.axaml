<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:AndroidTriviaGame.ViewModels"
             mc:Ignorable="d" d:DesignWidth="360" d:DesignHeight="800"
             x:Class="AndroidTriviaGame.Views.CreateLobbyView"
             x:DataType="vm:CreateLobbyViewModel">
    
    <Grid Background="#121214">
        
        <Grid VerticalAlignment="Top" Margin="16,16,16,0" ColumnDefinitions="Auto, *, Auto">
            
            <Button Grid.Column="0"
                    Content="← Back" 
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="#B3B3B3" 
                    Background="Transparent" 
                    BorderThickness="0"
                    Padding="8"
                    Command="{Binding BackCommand}"/>

            <Button Grid.Column="2"
                    Content="Join Lobby" 
                    FontSize="16"
                    FontWeight="Bold"
                    Foreground="#7C4DFF" 
                    Background="Transparent" 
                    BorderThickness="0"
                    Padding="8"
                    Command="{Binding JoinLobbyCommand}"/>
            
        </Grid>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch" Spacing="24" Margin="32">
            
            <StackPanel HorizontalAlignment="Center" Spacing="8">
                <TextBlock Text="✨" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="Create Lobby" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Spacing="16" Margin="0,16,0,0">
                <Button Content="Create" 
                        Height="56" 
                        HorizontalAlignment="Stretch" 
                        HorizontalContentAlignment="Center" 
                        VerticalContentAlignment="Center"
                        FontSize="18" 
                        FontWeight="Bold" 
                        Foreground="White" 
                        Background="#7C4DFF" 
                        CornerRadius="8"
                        Command="{Binding CreateCommand}"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>